import { Download, Plus, Share2, StickyNote, Undo2, Users } from "lucide-react";
import { Poster } from "../components/Poster";
import { heading } from "./helpers";
import { undoLastAction } from "../lib/ranking";
import { noteKey, hasNote } from "../lib/notes";
import type { ProfileViewProps } from "./types";

export function ProfileView({ profile, activeRanking, locale, t, label, format, setFormat, exportLayout, setExportLayout, exportProfile, share, shareUrl, qrUrl, profileName, setProfileName, namedProfile, persist, navigateTo, peer, editingRankIdx, setEditingRankIdx, editingRankTitle, setEditingRankTitle, renameRank, openCollection, shareSingleRanking, setActiveKind, ranking, setRanking, collection, notes, openNoteModal }: ProfileViewProps) {
  const profileRankIdx = profile.rankings.indexOf(activeRanking);
  const isRenamingProfile = editingRankIdx === profileRankIdx;
  return <>
    {heading(t("我的文化索引", "MY CULTURE INDEX"), profile.profileName, `${profile.rankings.length} ${t("个领域", "media")} / ${profile.rankings.reduce((count, entry) => count + entry.items.length, 0)} ${t("件作品", "works")}`)}<div style={{ display: "flex", justifyContent: "flex-end", marginBottom: 8, gap: 8, alignItems: "center" }}><button className="button secondary" title={t("批注此画像", "Annotate this profile")} onClick={() => openNoteModal(noteKey("profile", profile.profileId), profile.profileName, "film")} style={{ fontSize: 12, gap: 5, padding: "5px 12px", borderRadius: 999, color: hasNote(notes, noteKey("profile", profile.profileId)) ? "var(--accent)" : "var(--muted)", borderColor: hasNote(notes, noteKey("profile", profile.profileId)) ? "var(--accent)" : "var(--line)" }}><StickyNote size={14} />{hasNote(notes, noteKey("profile", profile.profileId)) ? t("画像批注", "Profile note") : t("添加画像批注", "Add profile note")}</button></div>
    {format === "png" && <div className="export-layout-switch"><span>{t("PNG 版式", "PNG layout")}</span><div className="segmented"><button className={exportLayout === "editorial" ? "active" : ""} onClick={() => setExportLayout("editorial")}>{t("编辑", "Editorial")}</button><button className={exportLayout === "collage" ? "active" : ""} onClick={() => setExportLayout("collage")}>{t("拼贴", "Collage")}</button><button className={exportLayout === "minimal" ? "active" : ""} onClick={() => setExportLayout("minimal")}>{t("极简", "Minimal")}</button></div></div>}
    <div className="profile-dimensions">{profile.rankings.map((entry, idx) => { const key = `${entry.kind}-${idx}`; return <button className={`profile-dimension medium-${entry.kind} ${activeRanking === entry ? "active" : ""}`} key={key} onClick={() => setActiveKind(key)}><Poster work={entry.items[0]} kind={entry.kind} /><span>{label(entry.kind)}</span><strong>{entry.collectionTitle}</strong><small>TOP {entry.items.length}</small></button>; })}</div>
    <div className="profile-layout"><section><div className="section-heading">{isRenamingProfile ? <div style={{ display: "flex", gap: 6, alignItems: "center" }}><input type="text" value={editingRankTitle} onChange={(e) => setEditingRankTitle(e.target.value)} onKeyDown={(e) => { if (e.key === "Enter") renameRank(profileRankIdx); if (e.key === "Escape") setEditingRankIdx(null); }} style={{ fontSize: 18, fontWeight: 600, padding: "4px 8px", minHeight: "auto", flex: 1 }} autoFocus /><button className="text-button" onClick={() => renameRank(profileRankIdx)} style={{ color: "var(--accent)", fontSize: 14 }}>✓</button></div> : <h2 style={{ display: "flex", alignItems: "center", gap: 10 }}>{activeRanking.collectionTitle}<button className="button secondary" title={t("批注此榜单", "Annotate this list")} onClick={() => openNoteModal(noteKey("ranking", activeRanking.kind, activeRanking.collectionTitle), activeRanking.collectionTitle, activeRanking.kind, [activeRanking.items[0]?.posterUrls?.[0] ?? ""])} style={{ fontSize: 11, gap: 4, padding: "3px 10px", borderRadius: 999, minHeight: "auto", color: hasNote(notes, noteKey("ranking", activeRanking.kind, activeRanking.collectionTitle)) ? "var(--accent)" : "var(--muted)", borderColor: hasNote(notes, noteKey("ranking", activeRanking.kind, activeRanking.collectionTitle)) ? "var(--accent)" : "var(--line)" }}><StickyNote size={12} />{hasNote(notes, noteKey("ranking", activeRanking.kind, activeRanking.collectionTitle)) ? t("批注", "Note") : t("添加批注", "Add note")}</button></h2>}<div style={{ display: "flex", gap: 8, alignItems: "center" }}><span>{new Date(activeRanking.createdAt).toLocaleDateString(locale === "zh" ? "zh-CN" : "en-US")}</span><button className="text-button" onClick={() => { setEditingRankIdx(profileRankIdx); setEditingRankTitle(activeRanking.collectionTitle); }} style={{ fontSize: 12, color: "var(--muted)" }}>{t("改名", "Rename")}</button><button className="text-button" onClick={() => { const works = activeRanking.items.map(item => ({ id: item.id, title: item.title, subtitle: item.subtitle, creator: item.creator, year: item.year, posterUrls: item.posterUrls })); openCollection({ id: `rerank-${activeRanking.profileId}`, kind: activeRanking.kind, source: "custom", title: activeRanking.collectionTitle, description: "", topN: activeRanking.items.length, works }); }} style={{ fontSize: 12, color: "var(--accent)" }}>{t("重新排序", "Re-rank")}</button><button className="text-button" onClick={() => void shareSingleRanking(activeRanking)} style={{ fontSize: 12, color: "var(--accent)" }}>{t("分享此榜单", "Share this list")}</button></div></div><ol className="ranking-list">{activeRanking.items.map((work) => <li key={work.id}><span className="row-number">{String(work.rank).padStart(2, "0")}</span><Poster work={work} kind={activeRanking.kind} /><div><strong>{work.title}</strong><small>{work.creator} {work.year}</small></div>{work.rank <= 3 && <span style={{ fontSize: work.rank === 1 ? 20 : 16 }}>{work.rank === 1 ? "🥇" : work.rank === 2 ? "🥈" : "🥉"}</span>}<button className="icon-button" title={t("批注", "Note")} onClick={(e) => { e.stopPropagation(); openNoteModal(noteKey("work", activeRanking.kind, work.id), work.title, activeRanking.kind, work.posterUrls); }} style={{ width: 28, height: 28, marginLeft: "auto", color: hasNote(notes, noteKey("work", activeRanking.kind, work.id)) ? "var(--accent)" : "var(--muted)", opacity: hasNote(notes, noteKey("work", activeRanking.kind, work.id)) ? 1 : 0.4 }}><StickyNote size={14} /></button></li>)}</ol></section><aside className="export-tools"><label htmlFor="profile-name">{t("画像名称", "Profile name")}</label><input id="profile-name" value={profileName} maxLength={80} onChange={(event) => { setProfileName(event.target.value); }} onBlur={() => { const next = namedProfile(); if (next) persist(next); }} /><label htmlFor="export-format">{t("导出格式", "Export format")}</label><select id="export-format" value={format} onChange={(event) => setFormat(event.target.value as typeof format)}>{["json", "png", "txt", "csv", "md"].map((item) => <option key={item} value={item}>{item.toUpperCase()}</option>)}</select><button className="button primary" onClick={exportProfile}><Download size={16} />{t("导出全部维度", "Export all media")}</button><button className="button secondary" onClick={share}><Share2 size={16} />{t("复制比较链接", "Copy comparison link")}</button>{shareUrl && <div className="share-output"><input aria-label={t("比较链接", "Comparison link")} readOnly value={shareUrl} onFocus={(event) => event.target.select()} />{qrUrl && <img src={qrUrl} alt={t("比较二维码", "Comparison QR code")} />}</div>}<button className="button quiet" onClick={() => navigateTo("home")}><Plus size={16} />{t("添加另一个维度", "Add another medium")}</button><button className="button quiet" onClick={() => navigateTo("compare")}><Users size={16} />{peer ? t("继续与好友比较", "Continue comparison") : t("与他人比较", "Compare with someone")}</button>{ranking?.completed && collection?.kind === activeRanking.kind && <button className="button quiet" onClick={() => { setRanking(undoLastAction(ranking)); navigateTo("sorting"); }}><Undo2 size={16} />{t("返回最后一次取舍", "Revisit last choice")}</button>}</aside></div>
  </>;
}
