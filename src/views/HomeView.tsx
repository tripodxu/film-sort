import { lazy, Suspense, useMemo, type ReactNode } from "react";
import { ArrowRight, BookOpen, Film, Library, Music2, Play, UserRound, Users } from "lucide-react";
import { getCollectionsByKind, mediaLabels, type MediaKind } from "../data/media";
import { Poster } from "../components/Poster";
import { fileInput } from "./helpers";
import type { HomeViewProps } from "./types";

const OrbScene = lazy(() => import("../components/OrbScene").then((module) => ({ default: module.OrbScene })));

const icons: Record<string, React.ComponentType<{ size?: number }>> = { film: Film, book: BookOpen, music: Music2, other: Library };
const kinds = Object.keys(mediaLabels) as MediaKind[];

export function HomeView({ locale, t, accountEmail, setAccountOpen, setShowGuide, kind, chooseKind, navigateTo, draft, resume, profile, label, setRingsLayout, ringsLayout, clearAllData, editingRankIdx, setEditingRankIdx, editingRankTitle, setEditingRankTitle, renameRank, deleteRank, setActiveKind, openCollection, importProfile }: HomeViewProps) {
  return <>
    <section className="orb-hero">
      <Suspense fallback={<div className="orb-scene orb-scene-fallback" aria-hidden="true" />}><OrbScene /></Suspense>
      <div className="orb-hero-inner">
        <div className="home-heading"><div><span className="eyebrow">YOUR PERSONAL CULTURE INDEX</span><h1>ART<span>/</span>RANK<small>{t("我的艺术人格", "My Artistic Profile")}</small></h1><p>{t("在两件作品之间，找到你真正想留下的那一个。", "Choose between two works and reveal what stays with you.")}</p></div></div>
        <div className="identity glass-capsule"><UserRound size={15} /><span>{accountEmail || t("本地游客", "Local guest")}</span><button className="text-button" onClick={() => setAccountOpen(true)}>{t("登录 / 同步", "Sign in / Sync")}<ArrowRight size={13} /></button></div>
        <button className="guide-help-btn" onClick={() => setShowGuide(true)} aria-label={t("使用说明", "Guide")} title={t("使用说明", "Guide")}>?</button>
        <div className="home-actions glass-capsule"><button className="button primary" onClick={() => chooseKind(kind)}><Play size={16} />{t("开始准备", "Start preparing")}</button><button className="button secondary" onClick={() => navigateTo("compare")}><Users size={16} />{t("开始相遇", "Start encounter")}</button>{draft && <button className="button quiet" onClick={resume}><Play size={16} />{t("继续上次进度", "Resume")}</button>}</div>
        <div className="orb-index" aria-hidden="true"><span>01</span><i /><span>∞</span></div>
      </div>
    </section>
    <section className="medium-section"><div className="section-heading"><h2>{t("选择一个维度", "Choose a medium")}</h2><span>01 / 04</span></div><div className="medium-grid">{kinds.map((item) => { const Icon = icons[item]; const saved = profile?.rankings.filter((entry) => entry.kind === item) ?? []; const latest = saved[0]; const builtinWorks = getCollectionsByKind(item).flatMap(c => c.works); const randomWork = useMemo(() => builtinWorks.length > 0 ? builtinWorks[Math.floor(Math.random() * builtinWorks.length)] : null, [builtinWorks.length > 0 ? builtinWorks[0]?.id : null]); return <button className={`medium-item medium-${item}`} key={item} onClick={() => chooseKind(item)}><span className="medium-number">{mediaLabels[item].symbol}</span>{latest ? <Poster work={latest.items[0]} kind={item} /> : randomWork ? <Poster work={randomWork} kind={item} /> : <Icon size={20} />}<strong>{label(item)}</strong><small>{saved.length > 0 ? `${saved.length}${t("个榜单", "lists")} / ${saved.reduce((s, r) => s + r.items.length, 0)}${t("件", "")}` : t(mediaLabels[item].description, "New ranking")}</small><ArrowRight size={16} /></button>; })}</div></section>
    <section className="profile-overview"><div className="section-heading"><h2>{t("品味年轮", "Taste Rings")}</h2><div style={{ display: "flex", gap: 12, alignItems: "center" }}>{profile && <button className="text-button" onClick={() => navigateTo("profile")}>{t("我的文化索引", "My Cultural Index")}<ArrowRight size={15} /></button>}{profile && <div style={{ display: "flex", gap: 4 }}>{["row","col"].map(m => <button key={m} onClick={() => { setRingsLayout(m as "row"|"col"); try { localStorage.setItem("art-rank:rings-layout", m); } catch {} }} style={{ width: 30, height: 30, borderRadius: 6, border: ringsLayout === m ? "1px solid var(--accent)" : "1px solid var(--line)", background: ringsLayout === m ? "rgba(216,248,106,.1)" : "transparent", color: ringsLayout === m ? "var(--accent)" : "var(--muted)", fontSize: 12, fontWeight: 600, cursor: "pointer", display: "flex", alignItems: "center", justifyContent: "center" }}>{m === "row" ? "≡" : "⋮"}</button>)}</div>}{profile && <button className="button quiet" onClick={clearAllData} style={{ fontSize: 11, padding: "4px 10px", borderRadius: 8, border: "1px solid var(--line)", color: "var(--muted)" }}>🗑 {t("清除数据", "Clear")}</button>}</div></div>{profile ? <div style={{ display: "flex", flexDirection: ringsLayout === "row" ? "column" : "row", gap: 20, flexWrap: "wrap" }}>{kinds.map(kind => { const entries = profile.rankings.map((r, i) => ({ ...r, _idx: i })).filter(r => r.kind === kind); if (entries.length === 0) return null; return <div key={kind} style={{ flex: ringsLayout === "row" ? "none" : "1 1 250px", minWidth: ringsLayout === "row" ? "auto" : 200 }}><div style={{ fontSize: 11, color: "var(--accent)", fontWeight: 600, marginBottom: 8, textTransform: "uppercase", letterSpacing: ".5px" }}>{label(kind as MediaKind)}</div><div style={{ display: "flex", flexDirection: ringsLayout === "row" ? "row" : "column", gap: 10, flexWrap: "wrap" }}>{entries.map(entry => { const idx = entry._idx; const isActive = editingRankIdx === idx; return <div key={`${entry.kind}-${entry.collectionTitle}-${idx}`} className="coordinate" style={{ position: "relative", flex: ringsLayout === "row" ? "0 0 auto" : "none" }}><div style={{ display: "flex", gap: 5, position: "absolute", top: 5, right: 5, zIndex: 2 }}><button onClick={(e) => { e.stopPropagation(); setEditingRankIdx(idx); setEditingRankTitle(entry.collectionTitle); }} title={t("重命名", "Rename")} style={{ width: 16, height: 16, borderRadius: "50%", background: "#f5c542", border: "1px solid #d4a830", cursor: "pointer", padding: 0 }} /><button onClick={(e) => { e.stopPropagation(); deleteRank(idx); }} title={t("删除", "Delete")} style={{ width: 16, height: 16, borderRadius: "50%", background: "#ff5f57", border: "1px solid #e04842", cursor: "pointer", padding: 0 }} /></div><button onClick={() => { setActiveKind(`${entry.kind}-${idx}`); navigateTo("profile"); }} style={{ display: "flex", gap: 12, alignItems: "center", flex: 1, background: "none", border: "none", color: "inherit", cursor: "pointer", padding: 0, textAlign: "left" }}><Poster work={entry.items[0]} kind={entry.kind} /><div>{isActive ? <div style={{ display: "flex", gap: 4, alignItems: "center" }}><input type="text" value={editingRankTitle} onChange={(e) => setEditingRankTitle(e.target.value)} onKeyDown={(e) => { if (e.key === "Enter") renameRank(idx); if (e.key === "Escape") setEditingRankIdx(null); }} style={{ fontSize: 12, padding: "2px 6px", minHeight: "auto", width: "100%" }} autoFocus /><button className="text-button" onClick={(e) => { e.stopPropagation(); renameRank(idx); }} style={{ color: "var(--accent)", fontSize: 11, padding: "0 4px" }}>✓</button></div> : <h3>{entry.collectionTitle}</h3>}<small>{entry.items.length} {t("件作品", "works")}</small></div></button></div>; })}</div></div>; }).filter(Boolean)}</div> : <div className="empty-profile"><div className="sample-covers" aria-hidden="true">{getCollectionsByKind("film")[1]?.works.filter((work) => work.posterUrls?.length).slice(0, 3).map((work) => <Poster key={work.id} work={work} kind="film" />)}</div><div><h3>{t("还没有完成的画像", "No completed profile yet")}</h3><span>{t("电影 · 书籍 · 音乐 · 其他", "Films · Books · Music · Other")}</span></div>{fileInput("own", t("导入我的画像", "Import my profile"), importProfile)}</div>}</section>
  </>;
}
